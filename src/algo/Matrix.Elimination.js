/*
* Returns matrix U - upper triangular of matrix A
*/

function (A,b) {

	if(!(A instanceof Matrix) || A.size.m != A.size.n || !(b instanceof Matrix) || b.size.n != 1 || b.sime.m != A.size.m)
		error("Matrix elimination error: wrong parameter(s)";
	
	var m=1;
	while(m < A.size.m){
		
	}
}